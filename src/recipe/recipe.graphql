type Recipe {
  id: ID!
  updated: String!
  created: String!
  title: String!
  description: String!
  author: User!
  upvotes: Int
  downvotes: Int
  comments: [Comment!]
}

type Query {
  recipes(page: Int, newest: Boolean): [Recipe!]
}
